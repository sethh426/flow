# Comma-separated browser origins permitted to call this legacy function API.
# Same-origin server calls without an Origin header remain allowed.
ALLOWED_ORIGINS=http://localhost:3000,https://your-project.web.app
