# Local product data directory exposed to the filesystem MCP server.
MCP_PRODUCT_DATA_PATH=

# Firebase project used by the optional Firebase MCP server.
MCP_FIREBASE_PROJECT_ID=

# Optional local development credential path. Prefer Application Default
# Credentials or workload identity in deployed environments.
MCP_FIREBASE_SERVICE_ACCOUNT_PATH=
